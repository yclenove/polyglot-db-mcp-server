# QA-001: v1.5 测试计划

> **版本**: v1.4.0 → v1.5.0 | **日期**: 2026-05-05 | **状态**: 待评审 | **作者**: 测试经理

---

## 1. 测试概览

### 1.1 版本测试目标

| 项目 | 当前 (v1.4.0) | 目标 (v1.5.0) |
|------|---------------|---------------|
| 工具数 | 73 | ≤ 80 |
| 测试数 | 241 | ≥ 296 |
| 新增测试 | - | ≥ 55 |
| 测试通过率 | 100% | 100% |

### 1.2 新功能测试范围

| 功能 | 优先级 | 新增测试目标 | 测试文件 |
|------|--------|-------------|----------|
| 数据脱敏模式 | P0 | ≥ 25 个 | `test/tools/masking.test.mjs`, `test/core/data-masking.test.mjs` |
| 智能查询建议 | P1 | ≥ 18 个 | `test/tools/advisor.test.mjs`, `test/core/query-suggest.test.mjs` |
| 查询回放与对比 | P1 | ≥ 15 个 | `test/tools/replay.test.mjs`, `test/core/query-replay.test.mjs` |

### 1.3 测试层次分布

| 层次 | 占比 | 说明 |
|------|------|------|
| 单元测试 | 70% | 核心层纯函数、规则引擎、正则匹配 |
| 集成测试 | 20% | 工具层与驱动层协作、多引擎覆盖 |
| E2E 测试 | 10% | MCP 工具端到端调用、环境变量配置生效 |

### 1.4 现有测试基线

当前 v1.4.0 测试文件分布在以下目录：

```
test/
├── core/                  # 核心层单元测试
│   ├── sql-guards.test.mjs
│   ├── redis-guards.test.mjs
│   ├── config.test.mjs
│   ├── registry.test.mjs
│   ├── audit.test.mjs
│   └── logger.test.mjs
├── tools/                 # 工具层测试
│   ├── sql.test.mjs
│   ├── mongo.test.mjs
│   ├── redis.test.mjs
│   └── connections.test.mjs
├── drivers/               # 驱动层测试
│   ├── mysql-driver.test.mjs
│   ├── postgres-driver.test.mjs
│   ├── mssql-driver.test.mjs
│   ├── oracle-driver.test.mjs
│   ├── mongo-driver.test.mjs
│   ├── redis-driver.test.mjs
│   └── timeout.test.mjs
├── integration/           # 集成测试（需数据库）
│   ├── mysql.test.mjs
│   ├── postgres.test.mjs
│   ├── mongodb.test.mjs
│   └── redis.test.mjs
└── helpers/               # 测试辅助
    ├── integration-setup.mjs
    └── test-config.mjs
```

**测试风格**：使用 Node.js 内置 `node:test` + `node:assert/strict`，ESM 模块，从 `dist/` 编译产物导入。

---

## 2. 测试用例矩阵

### 2.1 数据脱敏模块 (data-masking)

#### 2.1.1 单元测试 — 核心引擎

| ID | 名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|----|------|--------|---------|---------|---------|
| TC-MASK-001 | strict 模式下手机号脱敏 | P0 | `MaskingConfig.mode = 'strict'`；数据含 `phone: "13812345678"` | 1. 构造包含手机号的结果集<br>2. 调用 `applyMasking(data, config)` | 返回 `phone: "138****5678"` |
| TC-MASK-002 | strict 模式下邮箱脱敏 | P0 | `MaskingConfig.mode = 'strict'`；数据含 `email: "test@example.com"` | 1. 构造包含邮箱的结果集<br>2. 调用 `applyMasking(data, config)` | 返回 `email: "t***@example.com"` |
| TC-MASK-003 | strict 模式下身份证号脱敏 | P0 | `MaskingConfig.mode = 'strict'`；数据含 `id_card: "110101199001011234"` | 1. 构造包含身份证的结果集<br>2. 调用 `applyMasking(data, config)` | 返回 `id_card: "110101****1234"`（前6后4可见） |
| TC-MASK-004 | strict 模式下信用卡号脱敏 | P0 | `MaskingConfig.mode = 'strict'`；数据含 `credit_card: "6222021234567890"` | 1. 构造包含信用卡号的结果集<br>2. 调用 `applyMasking(data, config)` | 返回 `credit_card: "6222********7890"`（前4后4可见） |
| TC-MASK-005 | loose 模式下未知字段不脱敏 | P0 | `MaskingConfig.mode = 'loose'`；数据含 `nickname: "test_user"` | 1. 构造包含非敏感字段的结果集<br>2. 调用 `applyMasking(data, config)` | `nickname` 保持原值 `"test_user"` 不变 |
| TC-MASK-006 | 白名单字段不脱敏 | P0 | `MaskingConfig.excludeFields = ['phone']`；数据含 `phone: "13812345678"` | 1. 配置白名单排除 `phone`<br>2. 调用 `applyMasking(data, config)` | `phone` 保持原值 `"13812345678"` 不变 |
| TC-MASK-007 | 空结果集不报错 | P0 | `data = []`（空数组） | 1. 传入空数组<br>2. 调用 `applyMasking([], config)` | 返回空数组 `[]`，无异常抛出 |
| TC-MASK-008 | 嵌套对象递归脱敏 | P0 | 数据含嵌套结构 `user: { phone: "13812345678", profile: { email: "a@b.com" } }` | 1. 构造嵌套对象结果集<br>2. 调用 `applyMasking(data, config)` | 递归脱敏：`user.phone = "138****5678"`，`user.profile.email = "a***@b.com"` |
| TC-MASK-009 | 数组类型结果脱敏 | P1 | 数据含数组字段 `tags: ["13812345678", "normal_tag"]` | 1. 构造含数组字段的结果集<br>2. 调用 `applyMasking(data, config)` | 数组中手机号被脱敏，普通字符串保持不变 |
| TC-MASK-010 | 环境变量 `DB_MASKING_MODE` 配置生效 | P0 | 设置环境变量 `DB_MASKING_MODE=strict` | 1. 设置环境变量<br>2. 初始化配置<br>3. 读取 masking config | `mode` 为 `strict`，`enabled` 为 `true` |
| TC-MASK-011 | 环境变量 `DB_MASKING_EXCLUDE_FIELDS` 配置生效 | P0 | 设置 `DB_MASKING_EXCLUDE_FIELDS=phone,email` | 1. 设置环境变量<br>2. 初始化配置<br>3. 读取 masking config | `excludeFields` 包含 `['phone', 'email']` |
| TC-MASK-012 | 自定义规则生效 | P1 | 自定义规则：字段名匹配 `/order_id/i`，脱敏函数保留前4位 | 1. 注册自定义 MaskingRule<br>2. 数据含 `order_id: "ORD123456789"`<br>3. 调用 `applyMasking(data, config)` | `order_id` 按自定义规则脱敏 |
| TC-MASK-013 | strict 模式下银行卡号脱敏 | P1 | 数据含 `bank_card: "6222021234567890123"` | 1. 构造包含银行卡号的结果集<br>2. 调用 `applyMasking(data, config)` | 返回 `bank_card: "6222*********0123"` |
| TC-MASK-014 | strict 模式下 IP 地址脱敏 | P1 | 数据含 `ip_address: "192.168.1.100"` | 1. 构造包含 IP 地址的结果集<br>2. 调用 `applyMasking(data, config)` | 返回 `ip_address: "192.168.*.*"` |
| TC-MASK-015 | 字段名大小写不敏感匹配 | P1 | 数据含 `PHONE: "13812345678"` 和 `Phone: "13900001111"` | 1. 构造大小写混合字段名<br>2. 调用 `applyMasking(data, config)` | 两个字段均被正确脱敏 |

#### 2.1.2 集成测试 — 工具层

| ID | 名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|----|------|--------|---------|---------|---------|
| TC-MASK-016 | set_masking_mode 工具设置脱敏模式 | P0 | MCP server 已启动 | 1. 调用 `set_masking_mode` 工具，参数 `mode: 'strict', enabled: true`<br>2. 调用 `get_masking_config` 验证 | 返回 `{ mode: 'strict', enabled: true, rulesCount: 6 }` |
| TC-MASK-017 | get_masking_config 返回当前配置 | P0 | 已设置脱敏模式为 strict | 1. 调用 `get_masking_config` 工具 | 返回包含 `mode`, `enabled`, `rules`, `excludeFields` 的完整配置对象 |
| TC-MASK-018 | sql_query 启用 masking 参数脱敏生效 | P0 | MySQL 连接可用；表含敏感字段 | 1. 调用 `sql_query` 并传入 `masking: true`<br>2. 检查返回结果 | 敏感字段被脱敏，非敏感字段保持原值 |
| TC-MASK-019 | mongo_find 启用 masking 参数脱敏生效 | P0 | MongoDB 连接可用；集合含敏感字段 | 1. 调用 `mongo_find` 并传入 `masking: true`<br>2. 检查返回结果 | 敏感字段被脱敏 |
| TC-MASK-020 | 排除连接不脱敏 | P1 | 配置 `excludeConnections: ['prod-db']` | 1. 使用 `prod-db` 连接查询<br>2. 使用 `dev-db` 连接查询 | `prod-db` 结果不脱敏，`dev-db` 结果脱敏 |
| TC-MASK-021 | 脱敏不影响非敏感查询性能 | P1 | 配置 masking enabled，查询结果无敏感字段 | 1. 记录不启用 masking 的查询耗时<br>2. 记录启用 masking 的查询耗时<br>3. 对比 | 性能损耗 < 5%（纯函数遍历 O(n) 开销可忽略） |
| TC-MASK-022 | 大结果集脱敏不截断 | P1 | 查询返回 100 行（DB_MAX_ROWS） | 1. 调用 `sql_query` 返回 100 行<br>2. 启用 masking<br>3. 检查行数 | 返回行数不变（100 行），所有敏感字段已脱敏 |
| TC-MASK-023 | disabled 模式下不执行脱敏 | P0 | `MaskingConfig.enabled = false` | 1. 调用 `set_masking_mode` 设置 `enabled: false`<br>2. 执行 `sql_query` 并传入 `masking: true` | 返回原始数据，无脱敏处理 |

### 2.2 查询回放模块 (query-replay)

#### 2.2.1 单元测试 — 核心引擎

| ID | 名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|----|------|--------|---------|---------|---------|
| TC-RPLY-001 | 记录查询到历史 | P0 | `QueryHistory` 实例已创建 | 1. 构造 `QueryRecord`<br>2. 调用 `history.push(record)`<br>3. 调用 `history.list()` | 列表包含刚插入的记录 |
| TC-RPLY-002 | 获取历史列表 | P0 | 已插入 5 条记录 | 1. 调用 `history.list()` | 返回 5 条记录，按时间倒序排列 |
| TC-RPLY-003 | 按 ID 获取单条记录 | P0 | 已插入记录，id 为 `"q-001"` | 1. 调用 `history.getById("q-001")` | 返回对应的 `QueryRecord` 对象 |
| TC-RPLY-004 | 按 ID 获取不存在的记录返回 undefined | P1 | 历史为空或无此 ID | 1. 调用 `history.getById("nonexistent")` | 返回 `undefined` |
| TC-RPLY-005 | 环形缓冲区满时覆盖旧记录 | P0 | `maxSize = 3`，已插入 3 条记录 | 1. 插入第 4 条记录<br>2. 调用 `history.list()` | 列表包含最新 3 条记录，最早插入的记录被覆盖 |
| TC-RPLY-006 | 缓冲区大小配置生效 | P0 | 设置环境变量 `DB_REPLAY_BUFFER_SIZE=100` | 1. 设置环境变量<br>2. 创建 `QueryHistory` 实例<br>3. 插入 100 条记录<br>4. 插入第 101 条 | 缓冲区保持 100 条，最早记录被覆盖 |
| TC-RPLY-007 | list 方法支持 limit 参数 | P1 | 已插入 10 条记录 | 1. 调用 `history.list(5)` | 返回最近 5 条记录 |
| TC-RPLY-008 | QueryRecord 包含完整元数据 | P1 | 已插入一条查询记录 | 1. 获取记录<br>2. 检查字段完整性 | 记录包含 `id`, `timestamp`, `connectionId`, `engine`, `sql`, `params`, `resultSummary`, `executionTime`, `success` |
| TC-RPLY-009 | resultSummary 仅存采样行 | P1 | 原始结果 100 行 | 1. 插入记录<br>2. 检查 `resultSummary.sampleRows` | `sampleRows` 最多 5 行，`rowCount` 为 100，`fields` 包含所有列名 |

#### 2.2.2 集成测试 — 工具层

| ID | 名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|----|------|--------|---------|---------|---------|
| TC-RPLY-010 | query_history 工具返回历史列表 | P0 | 已执行过若干 SQL 查询 | 1. 调用 `query_history` 工具<br>2. 检查返回格式 | 返回 `QueryRecord[]` 数组，包含 `id`, `sql`, `timestamp` 等字段 |
| TC-RPLY-011 | query_history 按 connectionId 过滤 | P1 | 有来自不同连接的查询记录 | 1. 调用 `query_history` 并指定 `connectionId` | 仅返回该连接的查询记录 |
| TC-RPLY-012 | query_replay 回放查询返回正确结果 | P0 | 历史记录中有查询 ID `q-001` | 1. 调用 `query_replay` 工具，参数 `queryId: "q-001"`<br>2. 执行原查询对比 | 回放结果与原查询结果一致（相同 SQL + params） |
| TC-RPLY-013 | query_replay 不存在的 ID 报错 | P0 | 无此查询 ID | 1. 调用 `query_replay` 工具，参数 `queryId: "nonexistent"` | 返回错误信息，提示记录不存在 |
| TC-RPLY-014 | query_diff 对比两次查询结果差异 | P0 | 两次查询结果行数不同（如数据已更新） | 1. 分别记录两次查询<br>2. 调用 `query_diff` 工具 | 返回 `QueryDiffResult`：`added`, `removed`, `modified` 计数正确 |
| TC-RPLY-015 | query_diff 无差异时返回一致 | P0 | 两次相同查询 | 1. 执行相同查询两次<br>2. 调用 `query_diff` 对比 | 返回 `added: 0, removed: 0, modified: 0` |
| TC-RPLY-016 | 不同连接的查询分开记录 | P1 | 有 `mysql-conn` 和 `pg-conn` 两个连接 | 1. 分别在两个连接执行查询<br>2. 调用 `query_history` | 记录中 `connectionId` 正确区分不同连接 |
| TC-RPLY-017 | 回放失败查询记录 success=false | P1 | 历史中有一条失败的查询记录 | 1. 调用 `query_replay` 回放该记录<br>2. 检查结果 | 重新执行同样 SQL，返回执行结果（可能再次失败） |

### 2.3 智能查询建议模块 (query-suggest)

#### 2.3.1 单元测试 — 规则引擎

| ID | 名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|----|------|--------|---------|---------|---------|
| TC-SUGG-001 | 检测 SELECT * 并建议指定字段 | P0 | SQL: `SELECT * FROM users` | 1. 调用 `analyzeQuery(sql, 'mysql')` | 返回包含 `type: 'rewrite'` 的建议，消息提示"指定需要的列名，避免 SELECT *" |
| TC-SUGG-002 | 检测全表扫描并建议添加索引 | P0 | SQL: `SELECT * FROM users WHERE email = 'test@example.com'`；`email` 列无索引 | 1. 获取表结构信息<br>2. 调用 `analyzeQuery(sql, 'mysql', tableInfo)` | 返回包含 `type: 'index'` 的建议，消息提示"为 email 列创建索引" |
| TC-SUGG-003 | 检测前缀通配符并建议后缀匹配 | P0 | SQL: `SELECT * FROM users WHERE name LIKE '%test'` | 1. 调用 `analyzeQuery(sql, 'mysql')` | 返回包含 `type: 'performance'` 的建议，提示"前缀通配符 '%...' 无法使用索引" |
| TC-SUGG-004 | 正常查询无建议 | P0 | SQL: `SELECT id, name FROM users WHERE id = 1` | 1. 调用 `analyzeQuery(sql, 'mysql')` | 返回空建议数组 `[]` 或消息"当前查询无明显优化空间" |
| TC-SUGG-005 | EXPLAIN 分析结果整合 — 全表扫描检测 | P0 | EXPLAIN 结果含 `type: 'ALL'` | 1. 调用 `analyzeExplainPlan(plan, 'mysql')` | 返回包含 `type: 'performance'` 的建议，提示"检测到全表扫描" |
| TC-SUGG-006 | EXPLAIN 分析 — filesort 检测 | P1 | EXPLAIN 结果含 `Extra: 'Using filesort'` | 1. 调用 `analyzeExplainPlan(plan, 'mysql')` | 返回建议"考虑为 ORDER BY 列添加索引" |
| TC-SUGG-007 | EXPLAIN 分析 — 临时表检测 | P1 | EXPLAIN 结果含 `Extra: 'Using temporary'` | 1. 调用 `analyzeExplainPlan(plan, 'mysql')` | 返回建议"查询使用了临时表，考虑优化 GROUP BY/JOIN" |
| TC-SUGG-008 | 超时控制生效 | P0 | 设置 `DB_SUGGEST_TIMEOUT_MS=100` | 1. 触发复杂分析<br>2. 检查执行时间 | 分析在超时时间内返回（可能返回部分结果或超时提示） |
| TC-SUGG-009 | 非 SELECT 语句返回空建议 | P0 | SQL: `INSERT INTO t VALUES (1)` | 1. 调用 `analyzeQuery(sql, 'mysql')` | 返回空建议数组 `[]`（仅分析 SELECT 语句） |
| TC-SUGG-010 | 多条建议按优先级排序 | P0 | SQL 同时存在 SELECT *、前缀通配符、缺少索引 | 1. 调用 `analyzeQuery(sql, 'mysql', tableInfo)` | 建议按 severity 排序：`critical` > `warn` > `info` |
| TC-SUGG-011 | 子查询转 JOIN 建议 | P1 | SQL: `SELECT * FROM t1 WHERE id IN (SELECT id FROM t2)` | 1. 调用 `analyzeQuery(sql, 'mysql')` | 返回建议"考虑将子查询改为 JOIN 以提升性能" |
| TC-SUGG-012 | 缺少 WHERE 子句检测 | P1 | SQL: `DELETE FROM users`（无 WHERE） | 1. 调用 `analyzeQuery(sql, 'mysql')` | 返回 `type: 'security'` 建议，提示"DELETE/UPDATE 缺少 WHERE 子句" |
| TC-SUGG-013 | 不同引擎 EXPLAIN 格式兼容 | P1 | MySQL 和 PostgreSQL 的 EXPLAIN 输出格式不同 | 1. 分别传入 MySQL/PG 格式的 EXPLAIN<br>2. 调用 `analyzeExplainPlan` | 两种格式均能正确解析并给出建议 |
| TC-SUGG-014 | Suggestion 对象结构完整性 | P1 | 任意分析结果 | 1. 调用 `analyzeQuery`<br>2. 检查返回结构 | 每个 Suggestion 包含 `type`, `severity`, `message`；如有 SQL 优化则包含 `suggestedSql` |

#### 2.3.2 集成测试 — 工具层

| ID | 名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|----|------|--------|---------|---------|---------|
| TC-SUGG-015 | query_suggest 工具基本调用 | P0 | MCP server 已启动，数据库连接可用 | 1. 调用 `query_suggest` 工具，参数含 SQL 文本 | 返回 `Suggestion[]` 数组 |
| TC-SUGG-016 | query_suggest 指定 connectionId | P1 | 有多个数据库连接 | 1. 调用 `query_suggest` 并指定 `connectionId` | 使用对应连接的表结构信息分析 |
| TC-SUGG-017 | query_optimize 组合分析 | P0 | 数据库连接可用 | 1. 调用 `query_optimize` 工具<br>2. 检查返回 | 返回 `AnalysisResult`，包含 `suggestions` 和 `executionPlan` |
| TC-SUGG-018 | 无效 SQL 处理 | P1 | SQL 语法错误 | 1. 调用 `query_suggest` 传入非法 SQL | 返回错误提示，不抛出未处理异常 |

---

## 3. 回归测试策略

### 3.1 现有测试回归

| 项目 | 要求 | 验证方式 |
|------|------|---------|
| 现有 241 个测试 | 全部通过，零失败 | `npm test` 完整运行 |
| sql-guards 测试 | 所有注入检测、危险操作检查不变 | `node --test test/sql-guards.test.mjs` |
| redis-guards 测试 | 命令拦截逻辑不变 | `node --test test/redis-guards.test.mjs` |
| 工具层测试 | sql/mongo/redis/connections 测试不变 | 各工具测试文件独立运行 |
| 驱动层测试 | 6 个驱动 + timeout 测试不变 | `node --test test/drivers/*.test.mjs` |
| 集成测试 | MySQL/PG/MongoDB/Redis 集成测试不变 | Docker Compose 环境下运行 |

### 3.2 新功能对现有行为的影响评估

| 影响点 | 风险 | 验证方式 |
|--------|------|---------|
| `sql_query` 返回值处理 | 脱敏为可选参数，默认不启用 | 验证不传 `masking` 参数时返回值不变 |
| `mongo_find` 返回值处理 | 同上 | 同上 |
| `tools/sql.ts` 代码修改 | 查询历史记录逻辑新增 | 验证现有 sql_query 测试全部通过 |
| `tools/mongo.ts` 代码修改 | 同上 | 验证现有 mongo 测试全部通过 |
| `core/audit.ts` 扩展 | 查询回放复用审计模块 | 验证现有 audit 测试全部通过 |
| `server.ts` 工具注册 | 新增 7 个工具 | 验证工具总数 ≤ 80，现有工具注册不受影响 |

### 3.3 回归测试执行计划

| 阶段 | 时机 | 测试范围 | 预计耗时 |
|------|------|---------|---------|
| 开发阶段 | 每次提交 | 相关模块单元测试 | < 30s |
| 集成阶段 | 功能合并前 | 全量单元测试 + 集成测试 | < 2min |
| 发版前 | 版本冻结时 | 全量测试 + E2E + 性能基线对比 | < 10min |

---

## 4. 质量门禁

### 4.1 发版门禁条件

| 门禁项 | 阈值 | 优先级 |
|--------|------|--------|
| P0 用例通过率 | 100% | 阻塞 |
| P1 用例通过率 | ≥ 95% | 阻塞 |
| 新增测试数量 | ≥ 55 个 | 阻塞 |
| 总测试数量 | ≥ 296 个 | 阻塞 |
| 现有测试回归 | 241/241 通过 | 阻塞 |
| TypeScript 类型检查 | 零错误 | 阻塞 |
| ESLint 检查 | 零错误 | 阻塞 |
| 工具总数 | ≤ 80 | 阻塞 |
| 向后兼容性 | 现有工具参数不变 | 阻塞 |

### 4.2 新增测试统计

| 模块 | P0 | P1 | 合计 |
|------|----|----|------|
| 数据脱敏 — 单元测试 | 10 | 5 | 15 |
| 数据脱敏 — 集成测试 | 5 | 3 | 8 |
| 查询回放 — 单元测试 | 4 | 5 | 9 |
| 查询回放 — 集成测试 | 4 | 3 | 7 |
| 智能建议 — 单元测试 | 5 | 9 | 14 |
| 智能建议 — 集成测试 | 2 | 2 | 4 |
| **合计** | **30** | **27** | **57** |

> 57 ≥ 55（迭代计划要求），总测试数 241 + 57 = 298 ≥ 296，满足门禁。

### 4.3 测试文件规划

| 测试文件 | 测试数 | 覆盖用例 |
|----------|--------|---------|
| `test/core/data-masking.test.mjs` | 15 | TC-MASK-001 ~ TC-MASK-015 |
| `test/tools/masking.test.mjs` | 8 | TC-MASK-016 ~ TC-MASK-023 |
| `test/core/query-replay.test.mjs` | 9 | TC-RPLY-001 ~ TC-RPLY-009 |
| `test/tools/replay.test.mjs` | 7 | TC-RPLY-010 ~ TC-RPLY-017 |
| `test/core/query-suggest.test.mjs` | 14 | TC-SUGG-001 ~ TC-SUGG-014 |
| `test/tools/advisor.test.mjs` | 4 | TC-SUGG-015 ~ TC-SUGG-018 |

---

## 5. 测试执行指南

### 5.1 环境要求

| 依赖 | 版本 | 用途 |
|------|------|------|
| Node.js | ≥ 20 | 运行时 |
| Docker Compose | ≥ 2.20 | 集成测试数据库 |
| TypeScript 编译 | `npm run build` | 测试从 `dist/` 导入 |

### 5.2 执行命令

```bash
# 全量测试
npm test

# 单模块测试
node --test test/core/data-masking.test.mjs
node --test test/tools/masking.test.mjs
node --test test/core/query-replay.test.mjs
node --test test/tools/replay.test.mjs
node --test test/core/query-suggest.test.mjs
node --test test/tools/advisor.test.mjs

# 回归测试（仅现有测试）
node --test test/sql-guards.test.mjs test/redis-guards.test.mjs test/config.test.mjs test/registry.test.mjs test/audit.test.mjs test/logger.test.mjs test/schema.test.mjs

# 集成测试（需启动 Docker Compose）
docker compose up -d
node --test test/integration/*.test.mjs
```

### 5.3 测试风格规范

遵循项目现有风格（参考 `test/sql-guards.test.mjs`）：

```javascript
import assert from 'node:assert/strict';
import { describe, test } from 'node:test';
// 从编译产物导入
import { applyMasking } from '../dist/core/data-masking.js';

describe('applyMasking', () => {
  test('masks phone number in strict mode', () => {
    const data = [{ phone: '13812345678' }];
    const config = { mode: 'strict', enabled: true, rules: [], excludeFields: [] };
    const result = applyMasking(data, config);
    assert.equal(result[0].phone, '138****5678');
  });
});
```

---

## 6. 风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| 脱敏正则误匹配合法数据 | 中 | 中 | 白名单排除 + loose/strict 双模式 + 充分边界测试 |
| 查询建议依赖表结构，大表分析超时 | 低 | 低 | 超时控制 + 缓存表结构 + 单元测试不依赖真实数据库 |
| 查询结果快照内存占用 | 低 | 中 | 环形缓冲大小可配置 + 仅存摘要不存完整结果 |
| 新增工具影响现有工具注册 | 低 | 高 | 工具注册顺序不变 + 现有工具参数不变 + 回归测试覆盖 |

---

*待团队评审后确认。*

---

# v1.6 追加测试用例

> **版本**: v1.5.0 → v1.6.0 | **日期**: 2026-05-05 | **状态**: 待评审

## 7. v1.6 新增测试用例

### 7.1 环形缓冲优化 (F3)

| ID | 名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|----|------|--------|---------|---------|---------|
| TC-RING-001 | 缓冲满时覆盖最旧记录 | P0 | `QueryHistory` capacity=3 | 1. 插入 4 条记录<br>2. 调用 `list()` | 返回最新 3 条，最早记录被覆盖，`size` 始终 <= capacity |
| TC-RING-002 | head/tail 指针正确移动 | P0 | capacity=5 | 1. 插入 7 条记录<br>2. 验证 list 顺序和 getById | list 返回正确时序顺序；getById 对已覆盖记录返回 undefined |
| TC-RING-003 | 大量插入性能 | P0 | capacity=100 | 1. 循环插入 1000 条记录<br>2. 测量总耗时 | 总耗时 < 50ms，无 `Array.shift()` 调用（O(1) 操作） |
| TC-RING-004 | 并发插入安全性 | P1 | capacity=10 | 1. 使用 `Promise.all` 并发插入 50 条<br>2. 验证 size 和数据完整性 | size <= 10，无数据丢失或重复 |
| TC-RING-005 | 缓冲区大小配置生效 | P0 | 设置 `DB_REPLAY_BUFFER_SIZE=200` | 1. 创建 QueryHistory<br>2. 检查 capacity | capacity 为 200，默认值 50 不再生效 |

### 7.2 strict-v2 脱敏增强 (F4)

| ID | 名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|----|------|--------|---------|---------|---------|
| TC-V2-001 | strict-v2 值正则匹配生效 | P0 | mode='strict-v2'，数据含 `phone: "13812345678"` | 1. 构造结果集<br>2. 调用 `applyMasking` | phone 被脱敏为 `138****5678`（值匹配正则） |
| TC-V2-002 | strict-v2 字段名匹配生效 | P0 | mode='strict-v2'，数据含 `phone: "not-a-phone"` | 1. 构造结果集<br>2. 调用 `applyMasking` | phone 保持原值（字段名匹配但值不匹配正则，不脱敏） |
| TC-V2-003 | strict 向后兼容 | P0 | mode='strict'，数据含 `phone: "not-a-phone"` | 1. 构造结果集<br>2. 调用 `applyMasking` | phone 被脱敏（strict 模式仅按字段名，行为不变） |
| TC-V2-004 | 自定义规则在 strict-v2 下生效 | P1 | mode='strict-v2'，自定义规则匹配 `/order_id/i` | 1. 注册自定义规则<br>2. 数据含 `order_id: "ORD123"` | 按自定义规则脱敏 |
| TC-V2-005 | 非敏感值不误匹配 | P0 | mode='strict-v2'，数据含 `phone: "hello"` | 1. 构造结果集<br>2. 调用 `applyMasking` | phone 保持原值 `"hello"` 不变 |

### 7.3 审计日志导出 (F5)

| ID | 名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|----|------|--------|---------|---------|---------|
| TC-EXP-001 | 导出 JSON 格式正确 | P0 | 审计缓冲区有数据 | 1. 调用 `export_audit` 工具<br>2. 解析返回 JSON | 返回有效 JSON，包含 `logs` 数组和 `count` 字段 |
| TC-EXP-002 | limit 参数生效 | P0 | 缓冲区有 100 条记录 | 1. 调用 `export_audit({ limit: 10 })` | 返回恰好 10 条记录 |
| TC-EXP-003 | since 时间戳过滤生效 | P0 | 缓冲区有不同时间的记录 | 1. 记录当前时间 T<br>2. 插入新记录<br>3. 调用 `export_audit({ since: T })` | 仅返回时间 >= T 的记录 |

### 7.4 自定义脱敏规则管理 (F6)

| ID | 名称 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|----|------|--------|---------|---------|---------|
| TC-CUST-001 | 添加自定义规则 | P0 | 无自定义规则 | 1. 调用 `masking_rules` 添加规则<br>2. 查询验证 | 新规则生效，匹配字段被脱敏 |
| TC-CUST-002 | 删除自定义规则 | P0 | 已有自定义规则 | 1. 调用 `masking_rules` 删除规则<br>2. 查询验证 | 规则被移除，字段不再脱敏 |
| TC-CUST-003 | 列出所有规则 | P0 | 有内置 + 自定义规则 | 1. 调用 `masking_rules` 列表 | 返回内置规则 + 自定义规则完整列表 |
| TC-CUST-004 | 自定义与内置规则共存 | P0 | 有内置规则和自定义规则 | 1. 添加自定义规则<br>2. 查询含内置和自定义敏感字段 | 两类规则均生效，互不影响 |

## 8. v1.6 测试统计

| 模块 | P0 | P1 | 合计 |
|------|----|----|------|
| 环形缓冲优化 | 4 | 1 | 5 |
| strict-v2 脱敏 | 4 | 1 | 5 |
| 审计导出 | 3 | 0 | 3 |
| 自定义规则管理 | 4 | 0 | 4 |
| **v1.6 合计** | **15** | **2** | **17** |
| v1.5 基线 | 30 | 27 | 57 |
| **总测试目标** | **45** | **29** | **74** |

## 9. v1.6 测试文件规划

| 测试文件 | 测试数 | 覆盖用例 |
|----------|--------|---------|
| `test/core/ring-buffer.test.mjs` | 5 | TC-RING-001 ~ TC-RING-005 |
| `test/core/strict-v2.test.mjs` | 5 | TC-V2-001 ~ TC-V2-005 |
| `test/core/audit-export.test.mjs` | 3 | TC-EXP-001 ~ TC-EXP-003 |
| `test/core/custom-rules.test.mjs` | 4 | TC-CUST-001 ~ TC-CUST-004 |

## 10. v1.6 质量门禁

| 门禁项 | 阈值 | 优先级 |
|--------|------|--------|
| v1.6 新增测试通过率 | 100% | 阻塞 |
| v1.5 回归测试 | 57/57 通过 | 阻塞 |
| strict 模式向后兼容 | TC-V2-003 通过 | 阻塞 |
| 环形缓冲无 shift() 调用 | TC-RING-003 通过 | 阻塞 |

*待团队评审后确认。*
